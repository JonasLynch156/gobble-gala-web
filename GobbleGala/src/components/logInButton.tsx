import React from "react";

export const LogInButton = () => {

    function handleClick() {
        //alert(`I'm in!`);
    }

    return (
    <button onClick={handleClick}>
        Log me!
    </button>

    )
}